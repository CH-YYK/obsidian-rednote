{
	"name": "obsidian-rednote",
	"version": "1.1.8",
	"description": "Import Xiaohongshu (小红书) notes with media and categorization into Obsidian.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "node esbuild.config.mjs production",
		"install-local": "npm run build && node install-local.js",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "kylemumu",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^18.0.0",
		"esbuild": "^0.17.0",
		"obsidian": "latest",
		"tslib": "^2.4.0",
		"typescript": "^5.0.0"
	}
}
